#include "../include/brick.h"

int main(int argc, char *argv[]) {
  // SDL2Window window;
  // window.init();
  // window.foo();
  // window.update();
  // window.temporisation(4000);

  Brick brick;
  return 0;
}